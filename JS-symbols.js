const info = { name: 'Julia', age: 10, gender: 'F' };

// the Object is everything inside the curly braces like:
// < name: 'Julia', age: 10, gender: 'F' >

// the properties is name: 'Julia'
// the key is name
// the value is 'julia' typeOf string.
